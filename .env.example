# Variáveis de Ambiente - SentinelWeb
# Copie este arquivo para .env e configure conforme necessário

# Banco de Dados
DATABASE_URL=sqlite:///./sentinelweb.db
# Para PostgreSQL em produção:
# DATABASE_URL=postgresql://user:password@localhost:5432/sentinelweb

# Redis (Message Broker)
REDIS_URL=redis://localhost:6379/0

# Segurança
SECRET_KEY=sentinelweb-secret-key-change-in-production-2024
# IMPORTANTE: Em produção, gere uma chave segura com:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# Telegram Bot (Notificações)
# Obtenha o token no @BotFather
# Veja TELEGRAM_SETUP.md para instruções completas
TELEGRAM_BOT_TOKEN=

# Google PageSpeed Insights API (Performance Monitoring)
# Como obter a API Key GRATUITA:
# 1. Acesse: https://console.cloud.google.com/
# 2. Crie um novo projeto ou selecione um existente
# 3. Ative a API PageSpeed Insights: 
#    https://console.cloud.google.com/apis/library/pagespeedonline.googleapis.com
# 4. Crie credenciais (API Key):
#    APIs & Services > Credentials > Create Credentials > API Key
# 5. (Recomendado) Restrinja a chave apenas para PageSpeed Insights API
#
# Quota Gratuita: 25,000 requisições/dia
# Suficiente para ~833 sites (1 auditoria/dia por site)
GOOGLE_PAGESPEED_API_KEY=

# FastAPI
DEBUG=True
